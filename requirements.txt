# --- core (steps 1-2 need only these) ---
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- web + auth (step 6) ---
fastapi>=0.111
uvicorn[standard]>=0.30
authlib>=1.3
itsdangerous>=2.2
python-multipart>=0.0.9

# --- drafting (step 4) ---
anthropic>=0.40

# --- scheduler (step 7) ---
apscheduler>=3.10
